(function ($) { $.fn.animated = function (inEffect) { const $this = $(this); const el = $this.offset().top; let windwHeight = $(window).height(); let pageHeight; $this.css('opacity', '0'); $(window).on('scroll', function () { windwHeight = $(window).height(); pageHeight = $(window).scrollTop() + windwHeight * 0.6; if (pageHeight > el) { $this.addClass('animated'); $this.addClass(inEffect).css('opacity', '1') } }) } })(jQuery)
